# The bundled broker intentionally serves plaintext AMQP only on the private Compose/Kubernetes
# network. Do not expose port 5672. External brokers should use per-service amqps:// URLs.
listeners.tcp.default = 5672
loopback_users.guest = true
consumer_timeout = 3600000
